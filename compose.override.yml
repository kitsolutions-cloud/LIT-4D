services:
  email-provider:
    labels:
      project: lit-4d
    ports:
      - '1025:1025'
    command:
      - --log-mail-contents

  oauth-provider:
    labels:
      project: lit-4d
    environment:
      KC_HTTP_MANAGEMENT_PORT: 9101
      KC_HTTP_MANAGEMENT_RELATIVE_PATH: '/'
    ports:
      - '8180:8180'
      - '9101:9101'
    command:
      - start-dev
      - --import-realm
      # - start
      # - --import-realm
      # - --hostname=witty-alpaca-current.ngrok-free.app
      # - --proxy-headers=xforwarded
      # - --http-enabled=true

  sqldb-provider:
    labels:
      project: lit-4d
    ports:
      - '5432:5432'

  aws-services-provider:
    labels:
      project: lit-4d
    ports:
      - '127.0.0.1:4566:4566'
  
  mongodb-provider:
    labels:
      project: lit-4d
    ports:
      - '27017:27017'
  
  feature-flag-provider:
    labels:
      project: lit-4d
    ports:
      - '3000:3000'
      - '3100:3100'

  sqldbmngr_provider:
    labels:
      project: lit-4d
    environment:
      - ADMINER_DESIGN=pepa-linha-dark
    image: adminer
    ports:
      - '5433:8080'

include:
  - path: ./monitoring-provider/compose.override.yml
# No external dependencies required for the current automation scripts.
# All scripts use Python standard libraries (os, shutil, pathlib, argparse, importlib, sys).
ty >= 0.0.29, < 0.1.0
python-dotenv >= 1.2.2, < 1.3.0